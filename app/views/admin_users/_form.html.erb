<%= form_with(model: admin_user, class: "contents") do |form| %>
  <% if admin_user.errors.any? %>
    <div id="error_explanation" class="text-base text-red-950 px-3 py-2 font-medium rounded-lg mt-3">
      <p class="text-lg"><%= t('error', count: admin_user.errors.count) %> <%= t('forms.messages.prohibited this record from being saved') %>:</p>
      <ul>
        <% admin_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :email %>
    <%= form.text_field :email, class: "input_text_field", required: true %>
  </div>

  <div class="inline">
    <%= form.submit t('forms.buttons.submit'), class: "primary_button" %>
  </div>
  <%= link_to admin_users_path do %>
    <span class="tertiary_button"><%= t('forms.buttons.cancel') %>
      <i class="fa fa-times" aria-hidden="true"></i>
    </span>
  <% end %>
<% end %>
