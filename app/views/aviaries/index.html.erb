<div class="w-full">
  <% content_for :title, "Aviaries" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Aviaries</h1>
    <%= link_to t('menu.new'), new_aviary_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <%= turbo_frame_tag "aviaries" do %>
    <div class="min-w-full mb-10" id="aviary_listing">
      <%= render 'aviary_list' %>
    </div>
  <% end %>
</div>
