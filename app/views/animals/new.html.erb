<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= t('text.New') %> <%= t('text.Citizen') %>
    <% if @duplicate_animal_id.present? %>
      (<%= t('text.Duplicate') %>)
    <% end %>
  </h1>

  <%= render "form", animal: @animal %>

</div>
