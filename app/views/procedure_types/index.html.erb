<div class="w-full">

  <% content_for :title, "Procedure types" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl"><%= t('text.Procedure Types') %></h1>
    <%= link_to t('menu.new'), new_procedure_type_path, class: "primary_button" %>
  </div>

  <div id="procedure_types" class="min-w-full">
     <%= render 'type_list' %>
  </div>
</div>
