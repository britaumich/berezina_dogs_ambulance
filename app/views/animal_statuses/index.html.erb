<div class="w-full">

  <% content_for :title, t('text.statuses')  %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl"><%= t('text.Animal Status') %></h1>
    <%= link_to t('menu.new'), new_animal_status_path, class: "primary_button" %>
  </div>

  <div id="animal_statuses" class="min-w-full">
    <%= render 'status_list' %>
  </div>
</div>
